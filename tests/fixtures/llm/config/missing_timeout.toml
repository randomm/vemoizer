# [llm] section present but timeout_seconds missing → invalid (httpx
# defaults to timeout=None = hang; invariant: always set). Returns None.
[llm]
base_url = "https://api.example.com/v1"
model = "gpt-4o-mini"
api_key_env = "VEMOIZER_LLM_API_KEY"
